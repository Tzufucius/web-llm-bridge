# Agent Skills

Repository-maintained Agent Skills live here. Each Skill keeps its trigger rules and core workflow in `SKILL.md`, with detailed references loaded only when needed.

The current canonical Skill is [Web LLM Bridge](web-llm-bridge/SKILL.md). It is copied into the Python package by `python scripts/sync_skill_bundle.py`; edit the canonical tree rather than the generated mirror.
